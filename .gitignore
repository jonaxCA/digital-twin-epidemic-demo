# =============================================================================
# EPIDEMIA — Sistema Monolítico v0.1
# Lo que NUNCA debe subirse al repositorio.
# =============================================================================

# --- Secretos y configuración local ------------------------------------------
# .env trae DATABASE_URL (con contraseña) y JWT_SECRET_KEY. Solo se sube la
# plantilla .env.example, sin valores reales.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credenciales*.json
service-account*.json
secrets/

# Túneles / despliegue
ngrok.yml
.ngrok2/

# --- Respaldos de base de datos ----------------------------------------------
# Un pg_dump de una base en uso puede traer usuarios, hashes y casos reales.
# El esquema versionado vive en db/ y ese SÍ se sube.
*.dump
*.backup
*.bak
pg_dump_*.sql
respaldo*/
respaldos/
backups/

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# --- Archivo local -----------------------------------------------------------
# Pipeline y fuentes geográficas superadas (incluye un geojson de ~9 MB).
# Se conservan en disco, pero no forman parte del proyecto.
archivo/

# Descargas completas de INEGI (el zip del Marco Geoestadístico pesa ~98 MB);
# del repositorio solo se usa la capa datos/geo/inegi_mg2024/19mun.*
datos/geo/**/*.zip

# --- Logs y salidas temporales -----------------------------------------------
*.log
logs/
tmp/

# --- Editores y sistema operativo --------------------------------------------
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- CUDA --------------------------------------------------------------------
*.i
*.ii
*.gpu
*.ptx
*.cubin
*.fatbin

# --- Android -----------------------------------------------------------------
# build/ acotado a frontend_movil/ para no ocultar carpetas de otros componentes.
.gradle/
frontend_movil/**/build/
local.properties
captures/
.externalNativeBuild/
.cxx/
*.aab
*.apk
output-metadata.json
*.iml
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml
*.jks
*.keystore
google-services.json
*.hprof
